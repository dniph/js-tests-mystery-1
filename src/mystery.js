export const mystery1 = (num) => {
  return num % 2 !== 0;
};

// Negative odd numbers return a truthy result
// Odd positive numbers return a truthy result
// Zero will return a falsy result
// Even negative numbers must return a falsy result
// Even positive numbers must return a falsy result
